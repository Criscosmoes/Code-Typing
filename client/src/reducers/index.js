const initialState = {
    currentUser: {
        username: ""
    }
}


export default (state = initialState, action) => {

    switch(action.type) {
        default: 
            return state; 
    }
}